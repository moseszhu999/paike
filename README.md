# Private Exact-Head CI Carrier

This public repository carries only sanitized, opaque validation receipts for private-repository exact heads when private CI cannot start.

It must not contain private source code, strategy logic, raw data, credentials, repository file contents, or secret values. Carrier payloads are limited to commit/blob digests, generic scope counts, boolean safety boundaries, immutable artifact identifiers/digests, and synthetic contract tests.
